---
description: Pilot Tool 的调用规范（shell / MCP 二选一）
alwaysApply: true
---

# Tool 调用规范

Pilot 的 Tool 以**进程或 MCP 边界**暴露，不以 Python 函数边界暴露（见 `pilot/README.v2.md` §Scope & Positioning 核心守则）。Cursor 有两种调用方式。

## 方式 A：Shell（默认）

所有 Tool 都对应 `python -m pilot.tools.<name>` 子命令：

| Tool | 命令示例 |
|------|---------|
| `preflight.run` | `python -m pilot.tools.preflight run --cluster_id <id>` |
| `env_probe.run` | `python -m pilot.tools.env_probe run --cluster_id <id>` |
| `env_probe.sweep` | `python -m pilot.tools.env_probe sweep --base_plan <ref> --candidates <json>` |
| `submit.run` | `python -m pilot.tools.submit run --plan <ref>` |
| `submit.cancel` | `python -m pilot.tools.submit cancel --run_id <id>` |
| `observe.snapshot` | `python -m pilot.tools.observe snapshot --run_id <id>` |
| `observe.compare_loss` | `python -m pilot.tools.observe compare_loss --run_id <id> --reference <ref>` |
| `constraint.check` | `python -m pilot.tools.constraint check --plan <ref>` |
| `constraint.check_env` | `python -m pilot.tools.constraint check_env --env_diff <json>` |
| `constraint.estimate_mem` | `python -m pilot.tools.constraint estimate_mem --plan <ref>` |
| `state.checkpoint` | `python -m pilot.tools.state checkpoint --session <id> --stage <s>` |
| `state.resume` | `python -m pilot.tools.state resume --session <id>` |
| `state.trim` | `python -m pilot.tools.state trim --keep <fields>` |
| `state.handoff` | `python -m pilot.tools.state handoff --session <id> --reason <r>` |
| `knowledge.write` | `python -m pilot.tools.knowledge write --report <ref> --kind <k>` |

每个命令输出 JSON 到 stdout、日志到 stderr；你读 stdout 解析。

## 方式 B：MCP（可选）

若启用 `.cursor/mcp.json`（模板见 `pilot/integrations/cursor/mcp.json.example`），Pilot 会注册一组 MCP tool，与 shell 命令一一对应。MCP 的好处是参数带 schema，Cursor 自动做类型校验。

## 哪些你可以调 / 哪些不可以

按你的角色（见 `@.cursor/rules/10-orchestrator-role.mdc` / `@.cursor/rules/30-worker-*.mdc`）决定 scope：

| 角色 | 允许 |
|------|------|
| Orchestrator | 仅 `state.*` + `subagent.spawn`（= Cursor Task tool）|
| Diagnose Worker | `observe.*` / `profiler.*` / `state.plan_graph.*` |
| Re-Plan Worker | `constraint.*` / `state.plan_graph.*` |
| EnvSweep Worker | `env_probe.sweep` / `constraint.check_env` |
| Observe Worker | `observe.snapshot` / `observe.compare_loss` |
| Correctness-Lite | `observe.compare_loss` |

越权调用视为违反 `@.cursor/rules/00-pilot-core.mdc` 第 2 条（角色分离）。

## 共性约束

- 命令失败（非零退出码 / stderr 非空）→ 按 FailureReport（§8.8）处理，不要硬扛
- 所有写操作落到 `pilot/state/` 后**必须** `state.checkpoint()`
- 不要 inline Python 模拟 Tool 的行为（例如自己算 OOM 风险 → 调 `constraint.estimate_mem`）
