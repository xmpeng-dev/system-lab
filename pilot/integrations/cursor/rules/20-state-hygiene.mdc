---
description: State Layer 的读写规范与 handoff 协议
globs: pilot/state/**/*.yaml,pilot/state/**/*.json
alwaysApply: false
---

# State Layer 操作规范

`pilot/state/` 是 Pilot 唯一的工作记忆源。手动编辑 / 通过 Tool 读写都必须遵守以下规则。

## 禁止动作

- ❌ 直接 `cat`/`head`/`tail` 把完整 `tuning_state.yaml` 贴进 context
- ❌ 把 `candidate_pool.yaml` / `plan_graph.yaml` 全文塞给 Worker
- ❌ 跨 stage 保留 Snapshot / DiagnosisReport 的原始内容

## 正确做法

| 目的 | 命令 / 做法 |
|------|------------|
| 读当前 stage 指针 | `python -m pilot.tools.state summary` — 返回 < 500 tokens 的 YAML |
| 读 PlanGraph 的单节点 | `python -m pilot.tools.state plan_graph.node --id <plan_id>` |
| 读 CandidatePool 的 selected | `python -m pilot.tools.state candidate_pool.selected` |
| 让 Worker 读完整产物 | 只传 `ref: state/round_3/candidate_pool.yaml`，由 Worker 自己读 |
| 更新 champion | `python -m pilot.tools.state set_champion --id <plan_id>` |

## Checkpoint 协议

每 stage 出口：

```bash
python -m pilot.tools.state checkpoint \
  --session $SESSION_ID \
  --stage $STAGE \
  --round $ROUND
```

生成：
- `pilot/state/checkpoints/r$ROUND/tuning_state.yaml`
- `pilot/state/checkpoints/r$ROUND/plan_graph.yaml`（变化时）
- `pilot/state/checkpoints/r$ROUND/candidate_pool.yaml`（变化时）

## Resume 协议

新会话启动时：

```bash
python -m pilot.tools.state resume \
  --session $SESSION_ID
```

返回最后一次 checkpoint 的 `summary`；你按 `current_stage` 直接继续，不要重做已完成的 stage。

## Handoff 协议（Cursor 特化）

Cursor 无常驻进程，handoff 等价于"本次会话主动退出 + 下次从断点续起"。

触发条件：
- Orchestrator context > 50% window → **建议** handoff
- > 75% window → **强制** handoff

```bash
python -m pilot.tools.state handoff \
  --session $SESSION_ID \
  --reason "context_pressure" \
  --hint "$PENDING_DECISION"
```

落盘到 `pilot/state/checkpoints/handoff/<session_id>.yaml`，**提示用户下一次 Cursor 会话从同一 session_id 启动**，由 `state.resume()` 自动读接力点。

## gitignore

`pilot/state/` 整个目录应在 `.gitignore` 中。禁止提交。
