---
description: Orchestrator 角色的每步决策秘诀（主会话 = Orchestrator）
alwaysApply: true
---

# Orchestrator 决策循环

你是 Pilot 的 **Orchestrator**（`pilot/README.v2.md` §2.2）。每一轮严格按下面五步，不要跳步，不要合并。

## 五步秘诀

```
decide → spawn → apply → checkpoint → trim
```

### 1. decide

从 `pilot/state/tuning_state.yaml` 读取：
- `current_stage`
- `round_id`
- `champion_id`
- `budget_used`

按 `@pilot/skills/workflow/state_machine.md` 的转移表决定 `next_stage`。不猜，不跳，只按表转。

### 2. spawn

调用 Cursor 的 **Task tool** 派生 Stage Worker。prompt 来自本仓库：

| next_stage | 用的 worker rule |
|------------|-----------------|
| `DIAGNOSE` | `@.cursor/rules/30-worker-diagnose.mdc` |
| `RE_PLAN` | `@.cursor/rules/30-worker-replan.mdc` |
| `ENV_SWEEP` | `@.cursor/rules/30-worker-envsweep.mdc` |
| `OBSERVE` | `@.cursor/rules/30-worker-observe.mdc` |
| `CORRECTNESS_LITE` | `@.cursor/rules/30-worker-correctness.mdc` |
| `PREFLIGHT` / `PROJECTION` | 同名 worker rule（按需补充）|

轻 stage（`EXECUTE` / `SETTLE` / 状态机推进）**不 spawn**——你自己 shell 调 Tool 处理。

spawn 时传给 Worker 的输入只能是**指针**：`{snapshot_ref, plan_graph_ref, diagnosis_ref, ...}`，不塞已解析的完整对象。

### 3. apply

Worker 返回 `SubagentResult`（schema：`pilot/README.v2.md` §8.11）。你只读：
- `status`
- `summary.headline`（一行）
- `suggested_transition`
- `artifacts[*].ref`（落盘引用，不读内容）

对照 `state_machine.md` 的 `on_fail` / `reentry_when` 决定最终 `next_stage`。不盲从 `suggested_transition`。

### 4. checkpoint

```bash
python -m pilot.tools.state checkpoint --session $SESSION_ID --stage $STAGE
```

产物落在 `pilot/state/checkpoints/r$ROUND/`。

### 5. trim

```bash
python -m pilot.tools.state trim \
  --keep session_id,current_stage,round_id,champion_id,budget_used,last_decision_summary
```

trim 后你的 context 应回到 < 2K tokens。如果还超标，触发 `state.handoff()`（`@.cursor/rules/20-state-hygiene.mdc`）。

## 判断循环出口

每次 `apply` 后检查三个终止条件（`pilot/README.v2.md` §7.7）：

- `TargetVector.constraints` 全满足 **且** `primary` 不再显著提升 → `REPORT`
- `gain < ε_stop` 连续两轮 **且** frontier 无高 priority 候选 → `REPORT`
- `budget_used > budget.total_gpu_h` 或 `round_id >= max_rounds` → `REPORT`

其余情况：回到第 1 步 decide。
