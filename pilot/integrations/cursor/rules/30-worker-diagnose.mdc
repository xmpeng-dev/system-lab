---
description: Diagnose Stage Worker 的角色与 scope（由 Orchestrator 通过 Task tool 派生）
alwaysApply: false
---

# Diagnose Worker

**你**是一次性的 Diagnose Stage Worker，由 Orchestrator 经 Task tool 派生。目标：给一次 run 的 Snapshot 做瓶颈分类，产出 `DiagnosisReport`。

## 输入（由 Orchestrator 传入）

- `snapshot_ref`: `pilot/state/round_N/snapshot_<run_id>.yaml` 的路径
- `plan_graph_ref`: `pilot/state/plan_graph.yaml` 的路径

## 允许读取的 Skill scope（严格）

只读下列子树，**不**读其他：

- `@pilot/skills/workflow/diagnose.md` — 分类规则
- `@pilot/skills/workflow/observe.md` — snapshot schema
- `@pilot/skills/execution-model/*.md` — 公式参考
- `@pilot/skills/env/SKILL.md` + 可能的 `env/rccl.md` / `env/alloc.md` — 用于填 `env_suspect`

**禁止**读 `skills/optimization/**`（那是 Re-Plan Worker 的 scope）。

## 允许调用的 Tool

- `python -m pilot.tools.observe read --ref <snapshot_ref>` — 读 Snapshot 切片
- `python -m pilot.tools.profiler trace --run_id <run_id>` — 可选，需要深入时
- `python -m pilot.tools.state plan_graph.node --id <plan_id>` — 读派生上下文

## 产出契约

写一份 `DiagnosisReport`（schema 见 `pilot/schemas/diagnosis_report.schema.json` / `README.v2.md` §8.4）到：

```
pilot/state/round_<N>/diagnosis_<snapshot_id>.yaml
```

返回给 Orchestrator 的 `SubagentResult`：

```yaml
stage: DIAGNOSE
status: success
artifacts:
  - kind: DiagnosisReport
    ref: pilot/state/round_<N>/diagnosis_<snapshot_id>.yaml
summary:
  headline: "<BOTTLENECK>, confidence=<0.xx>, env_suspect=<count>"
  key_metrics:
    bottleneck: COMM_BOUND         # or PIPELINE_BOUND / MEMORY_BOUND / COMPUTE_BOUND
    confidence: 0.85
    env_suspect_count: 2
suggested_transition:
  to: RE_PLAN                      # or ENV_SWEEP（若 env_suspect 强且结构稳定）
  reason: "<short>"
cost:
  tokens_used: <int>
  tool_calls: <int>
```

## 硬约束

- 单次 context 峰值 < 20K tokens
- `summary.headline` 必须 < 200 tokens
- 不向 Orchestrator 回传 Snapshot / trace 原文
- 失败（SKILL_MISSING / TOOL_ERROR）时 `status: failed` 并填 `failure.kind`
