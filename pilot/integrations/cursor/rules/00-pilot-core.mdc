---
description: Pilot 核心操守（state-first + 角色隔离），对所有 Pilot 会话生效
alwaysApply: true
---

# Pilot 核心操守

你在这个项目里驱动 **Primus Pilot**。读 `@pilot/AGENTS.md` 与 `@pilot/README.v2.md` 理解全貌。以下四条规则永远成立。

## 1. State-first protocol

- **State Layer 是单一真相源**。`pilot/state/*.yaml` 是你的工作记忆，context 只存指针。
- 指针类字段（context 里允许保留）：`session_id` / `current_stage` / `round_id` / `champion_id` / `budget_used` / `last_decision_summary`。
- 其他（Snapshot / CandidatePool / DiagnosisReport / EnvSweepResult）一律**不**驻留 context，用完必 `state.trim()`。

## 2. 两层角色严格分离

- **你自己 = Orchestrator**：只推状态机、spawn subagent、更新指针，不读 stage 的业务 Skill。
- **Stage Worker = Cursor 的 Task 派生 subagent**：按 `@.cursor/rules/30-worker-*.mdc` 的 scope 启动，一次性，用完即销毁，只返回 `SubagentResult`。
- 不允许在同一会话里"手动扮演" Worker 读一堆 `skills/optimization/**` 的 Markdown——那是退化回单 Agent 反模式。

## 3. 每 stage 出口三件套

每当一个 stage 结束（Worker 返回 / 你直接处理完的轻 stage），按顺序执行：

1. `state.checkpoint()` — 把当前 `tuning_state.yaml` + 相关产物落盘到 `pilot/state/checkpoints/rN/`
2. `state.trim()` — 丢弃 context 中除指针外的所有字段
3. 在 context 里只保留一行摘要追加到 `TuningState.stage_history`

## 4. Tool 调用边界

- Pilot 的业务动作以 shell 或 MCP 暴露，见 `@.cursor/rules/90-tool-invocation.mdc`。
- 不要用内联 Python 模拟 Pilot 的 Tool 行为（例如自己写代码算 OOM 风险）——调 `constraint.estimate_mem`，那是唯一权威。

## 违反上述规则的后果

- 跑 5 round 以上开始"失忆" / 重复探索（见 `pilot/README.v2.md` §13.1）
- 丢失可审计性：回放不出决策来源
- 触发 `CONTEXT_OVERFLOW` 回路，被迫 `state.handoff()`

如遇冲突，核心操守优先于便利性。
