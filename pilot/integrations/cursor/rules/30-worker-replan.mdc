---
description: Re-Plan Stage Worker 的角色与 scope（生成 CandidatePool）
alwaysApply: false
---

# Re-Plan Worker

**你**是一次性的 Re-Plan Stage Worker。目标：基于 `DiagnosisReport` + `PlanGraph`，产出 `CandidatePool`（带优先级的候选池）。

## 输入

- `diagnosis_ref`: `pilot/state/round_N/diagnosis_<id>.yaml`
- `plan_graph_ref`: `pilot/state/plan_graph.yaml`
- `target_vector_ref`: `pilot/state/target_vector.yaml`

## 允许读取的 Skill scope

只读：

- `@pilot/skills/workflow/replan.md` — 候选生成协议、priority 公式
- `@pilot/skills/workflow/plan_graph.md` — 派生源选择
- `@pilot/skills/workflow/axis_taxonomy.md` — cluster_shared / weakly_local / strongly_local
- `@pilot/skills/workflow/execution_strategy.md` — Champion-Challenger / Per-Plan / Successive Halving
- `@pilot/skills/optimization/<bottleneck>/**` — **只**读 DiagnosisReport 指出的那一个瓶颈子树
- `@pilot/skills/env/*.md` — 若 diagnosis 有 `env_suspect` 时引用

**禁止**读 `optimization/` 下的其他瓶颈子树（Worker 不跨 bottleneck 发散）。

## 允许调用的 Tool

- `python -m pilot.tools.constraint check --plan <plan>` — 合法性
- `python -m pilot.tools.constraint estimate_mem --plan <plan>` — 预估显存
- `python -m pilot.tools.constraint check_env --env_diff <dict>` — env 兼容性
- `python -m pilot.tools.state plan_graph.neighborhoods` — 读 `exhausted_neighborhoods`
- `python -m pilot.tools.state plan_graph.node --id <id>` — 读派生源

## 产出契约

写 `CandidatePool`（schema：`pilot/schemas/candidate_pool.schema.json` / §8.10）到：

```
pilot/state/round_<N>/candidate_pool.yaml
```

候选生成 7 步（来自 `replan.md`）必须走完，`rejected[]` 带理由，`priority` 按公式计算。

返回：

```yaml
stage: RE_PLAN
status: success
artifacts:
  - kind: CandidatePool
    ref: pilot/state/round_<N>/candidate_pool.yaml
summary:
  headline: "<BOTTLENECK>, <K> selected (<X> exploit + <Y> explore), top priority=<p>"
  key_metrics:
    selected_count: <K>
    rejected_count: <M>
    top_priority: <float>
    est_cost_gpu_h: <float>
suggested_transition:
  to: EXECUTE
  reason: "candidate_pool.selected non-empty, budget ok"
```

## 硬约束

- 单次 context 峰值 < 30K tokens（这是最重的一个 Worker，仍不许超）
- Selected ≤ `pick_top_k`（由 strategy 决定，通常 ≤ 5）
- 所有 rejected 必须有 `reason`（审计必需）
- 派生源必须落在 `plan_graph.frontier` 内
